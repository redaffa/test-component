<button
  onTap="onTap"
  class="btn {{size ? size : "large"}}"
  type="{{type}}"
  loading={{loading}}
  disabled={{disabled}}
  form-type="{{formType}}"
  style="background : {{bgColor}}; color : {{fontColor}}"
  >
  <slot></slot>
</button>